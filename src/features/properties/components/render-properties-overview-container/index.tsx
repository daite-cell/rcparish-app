const RenderPropertiesOverviewContainer = ({ pathName }: { pathName: string | number | undefined }) => {
	return <div>RenderPropertiesOverviewContainer for {pathName}</div>;
};

export default RenderPropertiesOverviewContainer;
